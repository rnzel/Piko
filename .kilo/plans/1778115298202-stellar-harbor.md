## Phase 7: Testing Strategy

### 7.1 Unit Test Coverage
**File:** `services/__tests__/SyncOrchestrator.test.ts`

Key test scenarios:
- Sequential state transitions (no skipping)
- Concurrent initialize calls (idempotency)
- Abort controller on unmount
- Mutex prevents race conditions
- Realtime listener cleanup
- Retry mechanism with exponential backoff
- Error state transitions
- Write-through when state=READY
- No write-through when state=IDLE/ERROR

### 7.2 Integration Test Scenarios
**File:** `tests/integration/auth-sync-flow.test.ts`

Test flows:
1. Sign in → sync → sign out → sign in (full cycle)
2. Guest → auth migration → sync stable
3. Network failure during upload → retry → success
4. Rapid auth state changes (onAuthStateChanged races)
5. Component unmount during initialization
6. Multiple tabs/sessions with same account

### 7.3 Manual QA Checklist
- [ ] Tasks persist after sign out/in
- [ ] No duplicate tasks on refresh
- [ ] Realtime updates appear within 2s
- [ ] Offline mode: local changes sync when online
- [ ] Migration modal doesn't block initialization
- [ ] Sign-out clears all state
- [ ] No memory leaks (heap snapshot)
- [ ] UI doesn't show READY before data loads

## Phase 8: Migration Plan

### 8.1 Rollback Strategy
1. Keep `syncFacade.ts` as wrapper during transition
2. Feature flag `enableNewSync` in config
3. Gradual rollout: 10% → 50% → 100% users
4. Monitor error rates and data consistency
5. Quick rollback via feature flag

### 8.2 Data Migration Steps
1. Deploy SyncOrchestrator code (behind flag)
2. Run data validation script to check for inconsistencies
3. Enable for new sign-ups only (Day 1)
4. Enable for returning users (Day 3)
5. Monitor for 7 days, then remove old code

### 8.3 Monitoring Metrics
- `sync.initialize.duration` (P50, P95)
- `sync.uploadLocalData.errors`
- `sync.syncDown.errors`
- `sync.realtime.listener.errors`
- `sync.state.duration.in.ERROR`
- `sync.duplicate.tasks.detected`
- `sync.data.divergence.count`

## Summary of Changes

### Files Created
1. `services/SyncOrchestrator.ts` - Centralized sync lifecycle
2. `services/__tests__/SyncOrchestrator.test.ts` - Unit tests
3. `tests/integration/auth-sync-flow.test.ts` - Integration tests

### Files Modified
1. `contexts/AuthContext.tsx` - Delegates to orchestrator
2. `services/taskService.ts` - Uses orchestrator methods
3. `services/groupService.ts` - Uses orchestrator methods  
4. `services/storageService.ts` - Fix guest default (true → false)
5. `services/syncFacade.ts` - Deprecated, wraps orchestrator

### Key Improvements
- ✅ Single source of truth for sync state
- ✅ Mutex prevents race conditions between syncDown/uploadLocalData
- ✅ Abort controllers prevent setState on unmounted components
- ✅ Sequential state transitions (no skipping)
- ✅ Automatic retry with exponential backoff
- ✅ Realtime listeners properly cleaned up
- ✅ Atomic merge operations
- ✅ Error propagation instead of silent catch
- ✅ Guest default fixed (prevents auth user stuck in guest mode)
- ✅ No more duplicate initializations

### Bugs Fixed
- [x] _uid nullification race
- [x] _syncInProgress false sharing
- [x] Auth state transition races
- [x] syncState vs _currentSyncState desync
- [x] Realtime snapshot async races
- [x] Double deinitialize
- [x] initializeSync after unmount
- [x] _uid captured in stale closures
- [x] Migration modal early exit
- [x] uploadLocalData errors swallowed
- [x] syncDown errors ignored
- [x] Guest default true → false
- [x] onAuthStateChanged skips during migration
- [x] Missing unsubscribe before resubscribe
- [x] Listener attaches before _uid set
- [x] Concurrent write conflicts
- [x] Batch write failures
- [x] Hard delete vs soft delete inconsistency

### Architecture Changes
- Before: Fragmented across AuthContext, syncFacade, migrationService
- After: Centralized in SyncOrchestrator with clear ownership

### State Machine
```
IDLE → AUTHENTICATING → UPLOADING_LOCAL → HYDRATING → REALTIME_READY → READY
                                    ↓
                                 ERROR ←
```

All transitions are sequential, observable, and atomic.